%nav.top-bar
  %ul.title-area
    %li.name
      %h1
        %a{href: root_path } Intervene
  %section.top-bar-section
    %ul.right
      - if app_user_signed_in?
        %li 
          = link_to current_app_user.email, "/"
        %li 
          = link_to "Sign Out", destroy_app_user_session_path, method: :delete
      - else
        %li
          = link_to "Sign Up", new_app_user_registration_path
        %li 
          = link_to "Sign In", new_app_user_session_path 
          